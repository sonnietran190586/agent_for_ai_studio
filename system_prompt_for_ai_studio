====== BEGIN FILE 1: /AGENTS.md ======

# SYS_CONFIG: GeminiLatest(AIStudio) | Lang:VI | WebSearch:external_only | Time Default: System Time

rules: [strictly_follow_System_Instruction, 0_train_override, LOCAL_FIRST]
refs: [UI code -> /INSTRUCTIONS/UI_DESIGN.md, plan(create/update) + save docs : no need ask -> /INSTRUCTIONS/PLANNING.md, dev -> /INSTRUCTIONS/DEVELOPMENT.md]
priority: [Gemini_API_docs(/Document/* + codebase) > training_data(fallback_only)] # AUTO-UPDATE THIS ARRAY AFTER SAVING DOCS

sys_enforce: [Workflow, Arch, Guidelines] -> STRICT_SYNC(Global_System_Prompt) | 0_override

triggers:
  on_save_docs: "scan_new_doc -> identify_topic -> edit_file(/AGENTS.md) to append the specific topic (e.g., Stripe_API, Firebase_Rules) into local_docs(...) in the priority list"

auto_refactor:
  trigger: "file_length >= 200 lines"
  action: "MANDATORY OVERRIDE TO REFACTOR AND SPLIT INTO MULTIPLE FILES. No exceptions."
  strategy: [types->src/types/, static->src/constants.ts, logic->src/services/]

====== END FILE 1 ======

====== BEGIN FILE 2: /INSTRUCTIONS/PLANNING.md ======
DOCS & PLANNING RULES
[DOCS] Save requested docs to /Document (auto-create dir & select format).
[PLAN] File: Document/PLAN.md.
SAVE ONLY upon explicit user command/agreement ("Do it" = Agreed).
APPEND ONLY. NEVER overwrite/delete old plans.
Track status (active/dropped) to allow reverting.
====== END FILE 2 ======

====== BEGIN FILE 3: /INSTRUCTIONS/UI_DESIGN.md ======
UI DESIGN RULES
[MINDSET] Pick ONE: minimal-editorial | brutalist | retro-future | utilitarian | soft-luxury. Commit fully.
[TYPO]
BANNED: Inter, Roboto, Arial, system-ui. Pick max 2 distinct Google Fonts (e.g., Syne+Lora, SpaceMono+IBM).
Hierarchy: Headings = tracking-tight, leading-[1.2]. Body = leading-relaxed, max-w-[65ch] (mx-auto).
Modifiers: Uppercase MUST use text-xs + tracking-wider. Text-desc MUST reduce opacity/color.
[COLOR & TEXTURE]
BANNED: Tailwind default blue/teal, flat pure #000/#FFF. Use OKLCH or opinionated HEX.
Depth: NO flat solid bg. Use subtle noise (SVG), radial glow, or glassmorphism (bg-black/50 + backdrop-blur-sm).
Shadows: Use color-matched shadows, diffused (e.g., shadow-[0_8px_30px_rgba(X,X,X,0.12)]).
[LAYOUT & MATH STRICTIONS]
8pt Grid: Margin/Padding MUST divide by 4.
Break Symmetry: Asymmetric columns, overlapping, sticky scrolls. Negative space is a tool.
Margin Rule: Outer Margin > 2x Inner Padding.
Radius Math: Child_Radius = Parent_Radius - Padding. Avoid clipping!
[COMPONENTS]
Buttons: Asymmetric (px-4 py-2). MUST have: hover (bg), active (scale-95), disabled (opacity-50), and focus:ring.
Inputs: Visible labels. Focus ring MUST NOT trigger layout shift. Error state = red ring + text-[11px] + icon.
Cards: Combine at least 2 different card treatments per view.
[MOTION & UX]
Intro: Staggered fade-up (delay increments). Hover: 200ms ease-out. Reveals: 600ms. NO bouncing.
Load/Empty: Skeletons (animate-pulse) > Spinners. Empty State BẮT BUỘC có: [Icon mờ + Text + CTA Button].
====== END FILE 3 ======

====== BEGIN FILE 4: /INSTRUCTIONS/DEVELOPMENT.md ======
# DEVELOPMENT & SEARCH RULES
1. [UI_FIRST] Propose Web UI -> Await approval.
2. [REF] Use provided '@google/genai Models' list.
3. [CONTEXT] Priority: `/Document` > Latest Search/Training Data.
4. [STACK] Prefer Open Source/Github. Propose MOST SUITABLE version (not strictly newest).
5. [WEB_DEMO] Responsive + Tailwind CSS.
6. [NATIVE] Port ONLY if requested.
====== END FILE 4 ======
[/SYSTEM_INIT_PROTOCOL]
